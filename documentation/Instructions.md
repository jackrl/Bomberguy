**Intructions to get the project working
* Install icedtea-netx-common on ubuntu. It is needed because openjdk doesn't include jnlp

**In-game controls
* Use the arrow keys to move
* Use space to throw a bomb